const paginationConfig = {
	currentPage: 1,
	amountRowsOnPage: 10,
	pages: 0,
};

const dataConfig = {
	data: [],
	status: null,
};

export const config = {
  dataConfig,
  paginationConfig
}